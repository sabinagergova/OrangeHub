package com.skrill.interns.MatrixCalculator.matrix;

import java.math.BigDecimal;

public interface IPrinter {

    public void printMatrix(Matrix matrix, int dimention);
	public void printArray(BigDecimal arr[]);

}
